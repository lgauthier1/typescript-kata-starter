// fizzbuzz/index.test.ts
import { describe, it, expect } from "vitest"
import { Fizzbuzz } from "./index"

describe("fizzbuzz", () => {
  it("basic case (start with the simplest test)", () => {
    expect(true).toBe(true)
  })
})
